#include <stdio.h>
#include <stdlib.h>
#include <locale.H>

int main ()

{
    float c, f;
    setlocale (LC_ALL,"");

     printf ("Informe a temperatura em gruas  Fahrenheit:");
     scanf("%f",&f);

     c = (f-32)/1.8;

     printf ("A temperatura em gruas Celsius é igual a: %f\n",c);


    return 0;
}
